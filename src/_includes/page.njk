---
layout: base.njk
---
<div class="project-header">
    <p><em>{{ title }}</em>, ({{ dates }})
    <br>{{ tagline }}</p>
</div>


{% if image %}
    <div class="hero-image">
    <img src="{{ image }}" alt="{{ image-title }}">
    </div>
{% endif %}

<div class="project-text">
    {{ content | safe }}
</div>